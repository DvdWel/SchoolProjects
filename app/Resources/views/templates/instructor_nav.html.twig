{% extends 'base.html.twig' %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('homepage') }}">Training Factory</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('instructor_lessons') }}">Lessen <span class="sr-only">(current)</span></a>
                </li>
            </ul>
            <div class="container-fluid">
                <p class="label-primary">Welcome: {{ app.user.username }}</p>
                <a href="{{ path('logout') }}"><button class="btn btn-outline-secondary">logout</button></a>
                <a class="btn btn-outline-dark" href="#">Profile</a>
            </div>
        </div>
    </nav>
{% endblock %}
